version: 2
env: node
plugins:
  - name: node
    version: 16
build:
  steps:
    - name: Install Dependencies
      command: npm ci
    - name: Build
      command: npm run build
run:
  command: npm run start